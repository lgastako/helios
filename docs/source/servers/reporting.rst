################
Reporting Server
################

The reporting server provides an REST API for programmatic access to
the data stored by the collection servers as well as a web-based user
interface for generating dynamic reports and graphs using the same
API/data.
